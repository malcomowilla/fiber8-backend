<%= form_with(model: hotspot_plan) do |form| %>
  <% if hotspot_plan.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hotspot_plan.errors.count, "error") %> prohibited this hotspot_plan from being saved:</h2>

      <ul>
        <% hotspot_plan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :hotspot_subscribers, style: "display: block" %>
    <%= form.text_field :hotspot_subscribers %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
