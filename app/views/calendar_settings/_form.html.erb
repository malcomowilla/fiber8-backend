<%= form_with(model: calendar_setting) do |form| %>
  <% if calendar_setting.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(calendar_setting.errors.count, "error") %> prohibited this calendar_setting from being saved:</h2>

      <ul>
        <% calendar_setting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :start_in_hours, style: "display: block" %>
    <%= form.text_field :start_in_hours %>
  </div>

  <div>
    <%= form.label :start_in_minutes, style: "display: block" %>
    <%= form.text_field :start_in_minutes %>
  </div>

  <div>
    <%= form.label :account_id, style: "display: block" %>
    <%= form.number_field :account_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
