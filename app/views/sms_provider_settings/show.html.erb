<p style="color: green"><%= notice %></p>

<%= render @sms_provider_setting %>

<div>
  <%= link_to "Edit this sms provider setting", edit_sms_provider_setting_path(@sms_provider_setting) %> |
  <%= link_to "Back to sms provider settings", sms_provider_settings_path %>

  <%= button_to "Destroy this sms provider setting", @sms_provider_setting, method: :delete %>
</div>
