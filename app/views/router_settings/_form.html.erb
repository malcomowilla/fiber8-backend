<%= form_with(model: router_setting) do |form| %>
  <% if router_setting.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(router_setting.errors.count, "error") %> prohibited this router_setting from being saved:</h2>

      <ul>
        <% router_setting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :router_name, style: "display: block" %>
    <%= form.text_field :router_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
