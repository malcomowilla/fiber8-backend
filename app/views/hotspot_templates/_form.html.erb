<%= form_with(model: hotspot_template) do |form| %>
  <% if hotspot_template.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hotspot_template.errors.count, "error") %> prohibited this hotspot_template from being saved:</h2>

      <ul>
        <% hotspot_template.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :account_id, style: "display: block" %>
    <%= form.number_field :account_id %>
  </div>

  <div>
    <%= form.label :preview_image, style: "display: block" %>
    <%= form.text_field :preview_image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
