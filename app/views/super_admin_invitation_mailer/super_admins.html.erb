<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <style>
    /* Reset styles */
    body, html {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      line-height: 1.6;
      background-color: #f7f7f7;
    }

    /* Container */
    .email-container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      background-color: #ffffff;
      border: 1px solid #e0e0e0;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    /* Header */
    .header {
      text-align: center;
      padding: 20px 0;
    }

    .header img {
      max-width: 150px;
      height: auto;
    }

    .header h1 {
      font-size: 24px;
      color: #333333;
      margin-top: 10px;
    }

    /* Content */
    .content {
      padding: 20px;
      color: #555555;
    }

    .content p {
      margin: 0 0 15px;
    }

    .content a {
      display: inline-block;
      padding: 10px 20px;
      background-color: #007bff;
      color: #ffffff;
      text-decoration: none;
      border-radius: 4px;
      margin-top: 10px;
    }

    .content a:hover {
      background-color: #0056b3;
    }

    /* Credentials Section */
    .credentials {
      background-color: #f9f9f9;
      padding: 15px;
      border-radius: 4px;
      margin: 20px 0;
    }

    .credentials p {
      margin: 5px 0;
    }

    .credentials strong {
      color: #333333;
    }

    /* Footer */
    .footer {
      text-align: center;
      padding: 20px 0;
      color: #999999;
      font-size: 14px;
    }

    .footer p {
      margin: 0;
    }
  </style>
</head>
<body>
  <div class="email-container">
    <!-- Header with Company Logo -->
    <div class="header">
      <img src="<%= @company_photo %>" alt="<%= @company_name %> Logo">
      <h1>Welcome to <%= @company_name %>!</h1>
    </div>

    <!-- Content -->
    <div class="content">
      <p>Hello,</p>
      <p>You have been invited to join <strong><%= @company_name %></strong> as a <strong>super admin</strong>. Below are your login credentials:</p>

      <!-- Credentials Section -->
      <div class="credentials">
        <p><strong>Username:</strong> <%= @admin.email %></p>
        <p><strong>Temporary Password:</strong> <%= @admin.password %></p>
      </div>

      <p>Please click the button below to set your password and start using the platform.</p>
      <p>
        <a href="<%= @invitation_link %>">Set Your Password</a>
      </p>
      <p>If the button above doesn't work, copy and paste the following link into your browser:</p>
      <p><small><%= @invitation_link %></small></p>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p>Best Regards,</p>
      <p>The <%= @company_name %> Team</p>
    </div>
  </div>
</body>
</html>