<%= form_with(model: general_setting) do |form| %>
  <% if general_setting.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(general_setting.errors.count, "error") %> prohibited this general_setting from being saved:</h2>

      <ul>
        <% general_setting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :timezone, style: "display: block" %>
    <%= form.text_field :timezone %>
  </div>

  <div>
    <%= form.label :allowed_ips, style: "display: block" %>
    <%= form.text_field :allowed_ips %>
  </div>

  <div>
    <%= form.label :account_id, style: "display: block" %>
    <%= form.number_field :account_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
