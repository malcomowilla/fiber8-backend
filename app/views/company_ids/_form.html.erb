<%= form_with(model: company_id) do |form| %>
  <% if company_id.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(company_id.errors.count, "error") %> prohibited this company_id from being saved:</h2>

      <ul>
        <% company_id.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :company_id, style: "display: block" %>
    <%= form.text_field :company_id %>
  </div>

  <div>
    <%= form.label :account_id, style: "display: block" %>
    <%= form.number_field :account_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
