<%= form_with(model: license_setting) do |form| %>
  <% if license_setting.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(license_setting.errors.count, "error") %> prohibited this license_setting from being saved:</h2>

      <ul>
        <% license_setting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :expiry_warning_days, style: "display: block" %>
    <%= form.text_field :expiry_warning_days %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
