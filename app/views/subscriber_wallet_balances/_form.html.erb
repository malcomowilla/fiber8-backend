<%= form_with(model: subscriber_wallet_balance) do |form| %>
  <% if subscriber_wallet_balance.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscriber_wallet_balance.errors.count, "error") %> prohibited this subscriber_wallet_balance from being saved:</h2>

      <ul>
        <% subscriber_wallet_balance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.number_field :amount %>
  </div>

  <div>
    <%= form.label :account_id, style: "display: block" %>
    <%= form.number_field :account_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
